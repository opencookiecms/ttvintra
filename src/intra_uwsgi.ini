[uwsgi]
# full path to Django project's root directory
chdir            = /home/intrattv/ttvintra/src/
# Django's wsgi file
module           = intra.wsgi
# full path to python virtual env
home             = /home/intrattv/ttvintra/env
# enable uwsgi master process
master          = true
# maximum number of worker processes
processes       = 10
# the socket (use the full path to be safe
socket          = /home/intrattv/ttvintra/src/intra.sock
# socket permissions
chmod-socket    = 666
# clear environment on exit
vacuum          = true
# daemonize uwsgi and write messages into given log
daemonize       = /home/intrattv/uwsgi-emperor.log
